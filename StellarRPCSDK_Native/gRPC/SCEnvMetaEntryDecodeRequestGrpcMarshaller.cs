// Generated code - do not modify directly
using System;
using System.IO;
using System.Buffers;
using Grpc.Core;
using ProtoBuf;
using ProtoBuf.Meta;
using Stellar;

namespace Stellar.RPC.AOT
{
    /// <summary>Custom marshaller for Stellar.SCEnvMetaEntryDecodeRequest</summary>
    public static class SCEnvMetaEntryDecodeRequestGrpcMarshaller
    {
        // Static constructor to configure type
        static SCEnvMetaEntryDecodeRequestGrpcMarshaller()
        {
            ConfigureTypes();
        }

        /// <summary>Configure type serialization</summary>
        public static void ConfigureTypes()
        {
            // Get runtime type model

            var model = RuntimeTypeModel.Default;

            // Configure Stellar.SCEnvMetaEntryDecodeRequest
            if (!model.IsDefined(typeof(Stellar.SCEnvMetaEntryDecodeRequest)))
            {
                var metaType = model.Add(typeof(Stellar.SCEnvMetaEntryDecodeRequest), false);

                // Add all properties with their original ProtoMember numbers
                metaType.Add(1, "EncodedValue");
                metaType.UseConstructor = false;
            }
        }

        /// <summary>Marshaller for SCEnvMetaEntryDecodeRequest</summary>
        public static readonly Marshaller<Stellar.SCEnvMetaEntryDecodeRequest> SCEnvMetaEntryDecodeRequestMarshaller = Marshallers.Create<Stellar.SCEnvMetaEntryDecodeRequest>(
            (message, serializationContext) =>
            {
                try
                {
                    var ms = new MemoryStream();
                    Serializer.Serialize(ms, message);
                    var buffer = ms.ToArray();
                    serializationContext.Complete(buffer);
                }
                catch (Exception ex)
                {
                    throw new RpcException(new Status(StatusCode.Internal, $"Serialization error: {ex.Message}"));
                }
            },
            (deserializationContext) =>
            {
                try
                {
                    var buffer = deserializationContext.PayloadAsReadOnlySequence().ToArray();
                    using (var ms = new MemoryStream(buffer))
                    {
                        return Serializer.Deserialize<Stellar.SCEnvMetaEntryDecodeRequest>(ms);
                    }
                }
                catch (Exception ex)
                {
                    throw new RpcException(new Status(StatusCode.Internal, $"Deserialization error: {ex.Message}"));
                }
            });

    }
}
